source "https://rubygems.org"

# GitHub Pages gem - 包含Jekyll和所有必要插件
gem "github-pages", group: :jekyll_plugins

# Windows平台支持
platforms :mingw, :x64_mingw, :mswin, :jruby do
  gem "tzinfo", '>= 1', '< 3'
  gem "tzinfo-data"
end

# Windows文件监控优化
gem "wdm", "~> 0.1.1", platforms: [:mingw, :x64_mingw, :mswin]